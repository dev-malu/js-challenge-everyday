let freq = [{number: '', counter: 0}];

let arr = [1, 2, 3, 4, 2, 1, 1, 3, 2, 1, 1, 3, 2, 1];
for (let i = 0; i < arr.length; i++) {
  let obj = ;
  if(freq.number !== arr[i]) {
    obj.number = arr[i];
    obj.counter += 1;
  }
}
